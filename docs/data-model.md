# Data model

- `users` — email, role, department
- `workflow_requests` — title, description, status, requester
- `status_history` — transition log
- `request_comments` — review notes
- `audit_logs` — append-only audit events

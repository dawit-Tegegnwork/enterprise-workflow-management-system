# SQLite default for local dev; use PostgreSQL in Docker Compose or production.
WORKFLOW_DATABASE_URL=sqlite:///./workflow.db

# Change in production (Render sets this automatically).
WORKFLOW_SECRET_KEY=change-me-in-production
